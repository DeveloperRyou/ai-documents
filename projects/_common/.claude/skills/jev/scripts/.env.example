# Copy this file to .env (same folder) and fill in your real key.
# .env is gitignored -- run.py loads it automatically on every call
# and never overrides a key you've already exported in the shell.
OPENROUTER_API_KEY=sk-or-v1-...
